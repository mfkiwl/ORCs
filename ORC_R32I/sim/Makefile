TOPLEVEL_LANG = verilog
EXTRA_ARGS += --trace --trace-structs
VERILOG_SOURCES = $(shell pwd)/../source/ORC_R32I.v
TOPLEVEL = ORC_R32I
MODULE = test_ORC_R32I

include $(shell cocotb-config --makefiles)/Makefile.sim
